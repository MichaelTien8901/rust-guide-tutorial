[package]
name = "memory-management"
version = "0.1.0"
edition = "2021"
description = "Heapless collections, custom allocators, and memory pool patterns"
publish = false

[package.metadata]
tutorial-chapter = "part8/memory-management"

[dependencies]
